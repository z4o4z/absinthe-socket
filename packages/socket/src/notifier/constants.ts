// eslint-disable-next-line import/prefer-default-export
export enum RequestStatus {
  SENT = 'sent',
  PENDING = 'pending',
  sending = 'sending',
  CANCELED = 'canceled',
  CANCELING = 'canceling',
}
