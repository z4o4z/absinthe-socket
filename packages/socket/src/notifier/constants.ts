// eslint-disable-next-line import/prefer-default-export
export enum RequestStatus {
  CANCELED = 'canceled',
  CANCELING = 'canceling',
  PENDING = 'pending',
  SENT = 'sent',
  SENDING = 'sending',
}
